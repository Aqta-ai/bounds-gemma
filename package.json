{
  "name": "bounds-gemma",
  "version": "0.1.0",
  "description": "On-device Gemma 4 contextual PHI redaction toolkit for HIPAA Safe Harbor de-identification. Runs in a browser via WebLLM or locally via Ollama, never on a server.",
  "license": "Apache-2.0",
  "type": "module",
  "private": false,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "example:ollama": "tsx examples/runOllama.ts",
    "example:webllm": "echo 'Open examples/webllm.html in a browser (Chrome + WebGPU + cross-origin-isolated headers)'"
  },
  "devDependencies": {
    "@mlc-ai/web-llm": "^0.2.83",
    "@types/node": "^20.12.7",
    "tsx": "^4.7.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "peerDependencies": {
    "@mlc-ai/web-llm": "^0.2.79"
  },
  "peerDependenciesMeta": {
    "@mlc-ai/web-llm": {
      "optional": true
    }
  }
}
